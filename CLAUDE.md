# 漢字多維解構 — 字卡 project

## 呢個 project 做緊咩

將一隻中文字用**幾種唔同方式**解構，交叉核對之後，俾用戶自己攞去做術數層
（斗數十二宮／地支，例如 豕＝豬→亥）。

**分工要守死：**

| 邊個做 | 做咩 |
|---|---|
| **Claude（你）** | 搵文獻、解構字形結構、粵音同音字引申、三者 cross-check |
| **用戶** | 術數層推理。**你唔好做，唔好猜，唔好喺卡入面寫斗數／五行／八字嘅嘢。** |

你嘅工作係令部件、讀音、意思變成用戶可以直接攞去用嘅「鈎」。

## 語言

**同用戶溝通**：現代中文書面語（跟用戶個人設定）。

**字卡內容（`<字>.md`、網站、PDF）：一律現代中文書面語，唔准用口語。**
呢個係硬規矩 —— 成果係要俾長輩喺網頁上面睇嘅，口語會阻住閱讀。
專有名詞、古籍引文、字典原文**保持原文**，一個字都唔准改。

> 舊卡入面仲有廣東話嘅，要逐張改返做書面語（見下面「改寫舊卡」）。

## 做緊邊啲字

**單一來源係 `字庫.tsv`**，由 `charlist.py` 由三地姓名排行（`三地中文名字排行統計.xlsx`）
加姓氏榜（`姓氏榜.tsv`）自動合併、計優先分、對埋現有字卡嘅完成狀態產生，
唔好再手抄清單落呢個檔。

```bash
python3 charlist.py            # 重新產生 字庫.tsv，並印未做嘅頭 20 個（按優先分排）
python3 charlist.py --todo 10  # 淨係想睇頭 10 個
```

`字庫.tsv` 分四層：**一 內地名**（原本 28 隻獨立字清單，同呢層完全對應）、
**二 台灣名**、**三 姓氏**（內地／台灣官方 + 標明嘅二手來源，見 `姓氏榜.tsv` 檔頭）、
**四 香港名（非官方）**（NameChef 流量榜，優先分刻意最低）。首頁「字庫進度」
一節（`build_site.py` 嘅 `charlist_html()`）自動讀呢個檔顯示進度，已做嘅字連返去字卡，
唔使人手同步。

已完成：**靜**（v3）、**財**（v3）、**美**（v3）、**偉**（v2）、**娜**（v2）、**發**（v2）、**家**（v1）、**豪**（v1）。
家、豪、發、財、美**唔喺**「一 內地名」28 隻清單入面，唔計入字庫嘅進度統計。

> **仲要做嘅遷移**：偉、娜、發、家、豪五張要改做書面語兼升 v3；家、豪仲要先補做
> v2 嘅同音層同交叉核對。

每次做 5–6 隻，唔好一次過做晒。揀邊隻先做：跑 `charlist.py` 睇優先分，
或者自己想做邊隻都得——優先分係參考，唔係死規矩。

## 字卡格式 v3（照跟，唔好自己改）

睇 `靜.md` 做標準樣板。**一張卡分三層**，由淺入深：

```
---  ← YAML front matter：推理網絡圖嘅資料（第 1 層）
format: 3
char: 靜
jyutping: [zing6]
verdict: "一句總結，最多四十幾個字"
nodes:                                    # 節點：一幅圖最多 12 個
  - {id: qing, label: "青\n生＋丹", layer: 部件, ref: "1.3"}
  - {id: jing, label: "靜",          layer: 字,   ref: "1.2"}
  - {id: end,  label: "解釋到讀音\n解釋不到今義", layer: 結論, ref: "4.4"}
edges:                                    # 邊：由邊去邊，寫住證據級別
  - {from: qing, to: jing, label: "聲符（耕部）", evidence: 原文}
combo:                                    # 組合資料（組合圖同 1.7 靠佢）
  layout: 左右                            # 左右／上下／內外／包圍／獨體
  ratio: "左右各半"
  gloss: "審也（說文）"                    # 目標字自己嗰句
  form:  {part: 青, gloss: "四說未定"}     # 形符
  sound: {part: 爭, gloss: "四說未定"}     # 聲符
  same_form:                              # 橫軸：同形符換聲符
    - {char: 靖, with: 立, gloss: "立竫也"}
  same_sound:                             # 縱軸：同聲符換形符
    - {char: 淨, with: 水, gloss: "魯北城門池"}
  depth: 2                                # 組合樹幾多層
  note: "兩軸都長，所以誰表意沒有定論"
  downstream:                             # 1.8 用：呢隻字自己做部件去咗邊
    count: 7                              # 一個都冇都要寫 count: 0
    items:
      - {char: 瀞, with: 氵, gloss: "無垢薉也"}
    note: "對比：青 108 個、爭 30 個"
---
# 靜

## 總覽                                   ← 第 2 層：七格結果，每格 2–6 點
### 查字典的解釋
### 不同部份結構解釋
### 同族字                                 ← 自動配圖（兩條軸）
### 做部件時                               ← 自動配圖（下游字）
### 引申義
### 同音字引申
### 其他結果

## 詳細考證                                ← 第 3 層：網頁上摺埋，撳先展開
## 0. 基本資料      （表：部首、筆畫、Unicode、倉頡、粵音全部讀音、國音、中古音、上古音、異體字）
## 1. 結構層
   1.1 字形演變（甲骨→金文→大篆→小篆→楷書，每格圖 + 【觀察】）
   1.2 六書歸類（說文原文 → 各家講法 → 結論）
   1.3 部件分解 ← **拆法對照係重中之重，見下面第 6 條**
   1.4 部件橫向連結（呢個部件去咗邊啲字）
   1.5 異體字
   1.6 部件總表（層級／部件／字典義／粵音／出處，**拆到獨體象形為止**）
## 2. 意思層
   2.1 成隻字查字典（逐本列）
   2.2 部件逐個查字典
   2.3 結構 → 意思推導鏈
## 3. 同音層（粵音）
   3.1 同音同調字表（分 A 組同源／B 組純諧音）
   3.2 同音異調字（只列名，唔引申）
   3.3 引申
## 4. 交叉核對
   4.1 結構 ↔ 意思    4.2 字典 ↔ 字典    4.3 同音 ↔ 結構    4.4 一句總結
## 5. 來源清單
## 6. 存疑／未驗證
```

### front matter 幾個規矩

| 欄 | 規矩 |
|---|---|
| `layer` | 只可以係 `部件`／`字`／`意思`／`同音`／`結論`。最少要有一個 `結論` |
| `evidence` | 只可以係 `原文`／`觀察`／`分歧`／`引申`，即係四個標記 |
| `ref` | 章節號（`"1.3"`），節點會變成連結跳過去。**要用引號**，否則 YAML 當佢係數字 |
| `label` | 短。節點一行最多約 6 個中文字，用 `\n` 自己摺行。太長會迫到幅圖散晒 |
| 引號 | label 或者 verdict 入面有 `:` `#`，或者 `*` 開頭嘅，一定要用雙引號包住 |

**誠實檢查（`cardfmt.py` 會自動驗）**：一條邊標住 `原文`，佢連住嗰兩節就一定要真係有
`【原文】`。**幅圖唔可以講到硬過正文** —— 圖係最多人只望嗰一眼嘅嘢，
喺度將引申扮成考據，等於呃人。

### 總覽六格點寫

**次序係用戶定嘅：先出結果，之後才落基本資料同完整考據。**

| 格 | 放咩 |
|---|---|
| 查字典的解釋 | 今義、本義、字典之間嘅出入、書證夠唔夠 |
| 不同部份結構解釋 | 每個部件**自己**係咩、邊個表音邊個表意、有咩爭議 |
| 同族字 | 佢企喺邊兩個家族：橫軸同形符換聲符、縱軸同聲符換形符、兩軸長短（自動配圖） |
| 做部件時 | **反方向**：呢隻字自己做部件，走入咗邊啲字（自動配圖） |
| 引申義 | 由本義一路引申出咩，同埋唔可以咁引申嘅陷阱 |
| 同音字引申 | 同音同調字表、分族、最實同最鬆嘅幾條 |
| 其他結果 | 交叉核對嘅結論、資料本身嘅矛盾、最值得攞去用嘅鈎 |

- 每格 **2 至 6 點**，多過 6 點就唔算摘要。
- 每點一句起兩句止，**重點用粗體**，等人掃一眼就攞到。
- 唔好將詳細考證抄落嚟，要**濃縮**。詳細嘢喺第 3 層有。

### 改寫舊卡（v1／v2 → v3）

1. 全文改做書面語。**`【原文】` 引文一個字都唔准郁。**
2. 改完即刻跑 `python3 check_yuanwen.py <字>.md` —— 佢會同 git 上一版比對
   所有「…」引文，有一個字唔同就失敗。
3. 加 front matter 同「## 總覽」，再喺 `## 0. 基本資料` 之前插「## 詳細考證」。
4. `python3 build_site.py` 會自動驗格式。
5. v1 卡（冇同音層、冇交叉核對）要先補做 v2 嗰兩節，先至升 v3。

### 四個標記 — 呢個係成個 project 嘅命脈

| 標記 | 意思 |
|---|---|
| `【原文】` | 字典／古籍**原文照抄**，一個字都唔可以改 |
| `【觀察】` | **你親眼 Read 過字形圖**之後寫嘅。冇睇過圖就唔准用呢個標記 |
| `【分歧】` | 各家講法不一 —— **全部並列，唔好揀一個當定論** |
| `【引申】` | 同音推想，**唔係考據** |

**最大嘅風險係將聯想當考據。** 寫得似層層有道理唔等於有證據。每一句都要交代係邊一級。

### 3.1 同音字關係，只准四選一

1. **同源**（共用聲符）— 最實
2. **通假／聲訓**（文獻有記）
3. **民俗諧音**（有出處，例如 桂→貴 吉祥圖案）
4. **純諧音**（淨係讀音撞）— 最鬆

## 已驗證嘅來源（照用，唔好自己搵過）

| 用途 | 網址 | 注意 |
|---|---|---|
| 粵音全部讀音 + 同音字 | `humanum.arts.cuhk.edu.hk/Lexis/lexi-can/search.php?q=<Big5>` | **Big5 編碼**，要 `iconv -f big5 -t utf8` |
| 同音字全表 | `.../lexi-can/pho-rel.php?s1=<聲母>&s2=<韻母>&s3=<聲調>` | |
| 字形演變、部件、略說 | `humanum.arts.cuhk.edu.hk/Lexis/lexi-mf/search.php?word=<字>` | UTF-8，直接 curl |
| 說文原文、六書、上古音、同聲符系列 | `zh.wiktionary.org/wiki/<字>` | 有鄭張尚芳上古音表，好有用 |
| 義項、詞例 | `dict.revised.moe.edu.tw` | **釋義收喺 `<meta name="Description">` 入面**，睇下面 |
| 異體字 | `dict.variants.moe.edu.tw/search.jsp?QTP=0&WORD=<字>` | dictView 嘅 ID 成日唔啱，會 404 |
| 古文字圖 | `commons.wikimedia.org/wiki/Special:FilePath/<字>-oracle.svg?width=400` | 要 `curl -L`（會 302）；`-bronze` `-bigseal` `-seal` |

### 教育部重編國語辭典 — 兩個坑

1. **釋義唔喺 HTML body，喺 `<meta name="Description">`**。用 `moedict.py`（如果 repo 有）或者照抄佢個做法。
2. **dictView.jsp 會限速** — 回 HTTP 200 但 **0 bytes**。要 session cookie + referer + User-Agent + 重試。
   **唔好將失敗嘅檔存落 cache**，否則之後永遠唔會重試。

### 用唔到嘅（唔好再試）

zdic.net、ctext.org、小學堂

## 硬規矩

1. **【觀察】一定要真係 Read 過張圖。** 唔准靠文字描述推，唔准靠記憶作。
   呢個係用戶明確要求，亦係成個 project 最有價值嘅部分。
2. **每個判斷後面要註來源。**
3. **學界有分歧就全部並列**，唔好揀一個當定論。
4. **每張卡最尾要有「存疑／未驗證」**，老實列明邊啲係二手轉述、邊啲未親眼證實。
5. **唔准掂術數層。**
6. **1.3 一定要做「拆法對照」，呢個係用戶最重視嘅一節。**
   **同一隻字，唔同來源拆得唔同** —— 要並列，唔可以只揀一套：

   | 來源 | 佢答緊咩問題 |
   |---|---|
   | 《說文》 | **點解會咁造** —— 邊個係形符、邊個係聲符 |
   | 教育部楷書寫法規範（異體字字典「字樣說明」） | **應該點樣寫** —— 筆畫級嘅規定 |
   | CHISE IDS 分解式 | **視覺上點砌** —— ⿰⿱⿸ 等等 |
   | 古文字學界 | **本來係咩** —— 可能同今形完全唔同 |

   實例（差異可以好大）：
   - **發**：說文拆 2 件（弓＋癹），教育部拆 3 件（癶＋弓＋殳），連「弓」喺邊都唔同
   - **靜**：第一層三套一致（青＋爭），但第二層「青」點拆三套全部唔同 ——
     說文「生＋丹」、張富海「生＋井」、教育部楷書「龶＋月」。
     **「月」係楷化結果，字源上從來唔係月。**
   - **財**：三套完全一致 —— 咁都要明講「難得一致」，同埋解釋點解（兩件左右分明，冇穿插）

   **一定要引【原文】教育部異體字字典嘅「字樣說明」同「說文釋形」。**
   攞法：`search.jsp?QTP=0&WORD=<字>` 攞 dictView 嘅 ID → `dictView.jsp?ID=<id>&q=1`
   → 抽「說文釋形」「字樣說明」「異體字」三欄（會 503，要重試）。
   個站順便會俾埋完整異體字清單，比搜尋頁準。

   **核心一句：寫法規範同字源分析答緊兩條唔同嘅問題，唔可以互相代入。**
   人哋查字典見到「青之下作月」，好容易誤會成「青＝龶＋月」，
   然後拆出嚟三件嘢一件都解釋唔到個字點解係嗰個意思。

7. **0 節要有「部件組合」一行**（IDS 分解式，例如 ⿰貝才），
   資料嚟自 CHISE IDS。呢行係 1.3 拆法對照嘅索引。
8. **字卡內文一律現代書面語**，引文除外（見上面「語言」）。
9. **推理網絡圖嘅邊，證據級別唔可以高過正文實際有嘅標記。**

## 砌網站／PDF

```bash
python3 check_glyphs.py                  # 先檢查每隻字都有字型畫得出
python3 make_pdf_font.py                 # 整 PDF 專用字型（加咗新字要重跑）
fc-cache -f ~/.local/share/fonts
python3 build_site.py                    # 砌成個網站入 site/（可跟字名：build_site.py 靜）
python3 build_pdf.py 字卡.pdf            # 合訂本 PDF（由 site/all.html 出）
python3 build_pdf.py --each pdf          # 每隻字一份 → pdf/<字>.pdf
python3 check_pdf.py 字卡.pdf pdf/*.pdf  # 驗證每份 PDF 真係有中文
python3 check_site.py --shots shots      # 用瀏覽器逐頁檢查 + 影相
python3 check_yuanwen.py <字>.md         # 改過文字之後，驗【原文】冇被改
python3 archive_card.py 靜               # 升格式之前，封存返現有版本
python3 archive_card.py --list           # 睇下封存咗啲咩
python3 cardfmt.py                       # 淨係驗格式
python3 netgraph.py 靜 > /tmp/a.svg      # 淨係出推理網絡圖，方便逐幅執
python3 combograph.py 財                 # 淨係出組合圖
python3 charlist.py                      # 重新產生 字庫.tsv（揀下一批做邊隻用）
```

### 版本封存（用戶要求：已出版嘅唔好刪）

```
archive/<字>_v<N>.pdf     已出版過嘅舊版，永久保留，唔覆蓋
pdf/<字>.pdf              最新版
```

- **版本號 ＝ 格式版本**（v1 結構＋意思／v2 再加同音層同交叉核對／
  v3 再加推理網絡、總覽、組合、做部件時）。**唔係每次改字都封存** ——
  咁樣 repo 會爆，而且大部分改動唔值得留一版。
- **升級格式之前一定要先跑 `python3 archive_card.py <字>`**，
  否則舊版 PDF 會俾新版蓋過。跑咗之後先改 .md。
- `archive_card.py` **唔會覆蓋已經存在嘅檔** —— 已出版嘅嘢唔應該改。
- 網站每張卡底部會自動列出該字嘅所有舊版連結，師傅都㩒得到。
- CI 嗰句 `rm -rf pdf` **只清 pdf/，唔好掂 archive/**。

**網站係主要成果**，PDF 係副產品（方便列印同傳送）。兩種 PDF 都要出：
`字卡.pdf` 係合訂本，`pdf/<字>.pdf` 係每隻字一份。兩樣都 commit 入 repo。

Push 上 main 之後 GitHub Actions 會自動做晒呢幾步，出去 GitHub Pages：
**https://ckckkckccom-cpu.github.io/zika/**

要裝一次：`sudo apt-get install -y fonts-hanazono`（罕見字用）

### 網站設計嘅硬規矩（讀者係長者）

1. **基礎字級 17px**，全部用 `rem`。有 A−／A+ 四級（17／20／24／28），設定會記住。
   起始唔使大 —— 用戶明確講過，因為隨時放得大。
2. **只有淺色高對比**，唔跟系統深色模式。
3. **掣最少 48px 高。**
4. **手機上一律唔准橫向捲。** 文字表格逐列堆疊（`data-label`）；
   **有圖嘅表格同字形演變圖帶改為摺行格子**（手機三欄、電腦四欄），
   一眼睇得晒，想睇大就按圖放大。用戶明確講過碌左碌右唔好。
   注意 `.t-img` 嘅 `tbody` 要留 `display:block` —— tbody 同 tr 一齊做 grid
   會變成格子套格子，啲 td 會爆出去。
5. **所有連結一律相對路徑。** Pages 喺 `/zika/` 子路徑，寫 `/assets/…` 會喺本機
   正常、上線爛晒。`build_site.py` 嘅 `check_links()` 會捉。
6. **推理網絡圖唔可以靠縮細去遷就手機** —— 咁做等於將 26px 嘅字縮到 12px。
   幅圖嘅邏輯闊度特登整窄（480），手機上接近 1:1，電腦上反而放大。
7. **部件要做兩個方向，唔好淨係拆開兩邊講。**

   **1.7 字族定位（向旁）**：一隻形聲字同時屬於兩個家族 ——
   橫軸（同形符換聲符）、縱軸（同聲符換形符）。**兩軸長短差異本身就係資料**
   （「財」嘅「貝」帶出成堆字、「才」只帶出三個；「靜」兩邊都長，
   所以先至會四說並列）。要有：組合方式表、兩軸嘅《說文》原文表、
   交叉點嘅發現、部件喺其他字嘅位置同變形、換位異體。

   **1.8 這個字做部件時（向下）**：**呢隻字自己走入咗邊啲字、做緊咩。**
   用 CHISE IDS 資料庫數（88,939 條，`awk -F'\t' '$3 ~ /X/ && $2 != "X"'`），
   同佢自己嘅部件對比。呢個落差成日好大：
   「財」2 個（兩個都冇釋義）、「靜」7 個（得「瀞」有內容）、
   「發」32 個（廢撥潑醱）、「貝」685 個、「青」108 個。
   **用戶要嘅就係呢個方向** —— 佢想知呢隻字做部件時帶咩意思。
   如果答案係「幾乎唔出現」，就要明講，並指返去應該追邊個部件。

   兩幅圖都用**會摺行嘅 HTML**，唔用 SVG —— 五格橫排喺手機一定要碌左右。

   **會意字冇聲符**，唔可以套形聲嗰兩條軸。front matter 寫 `kind: 會意`，
   組合圖會自動改用「同上半／同下半」呢類中性講法（見 `美.md`）。
8. **大字下面即刻要有一條密集資料格**（仿漢典）：粵音、國音、部首、筆畫、
   Unicode、倉頡、中古音、上古音…細字細格細線，佔高度少但資訊量大。
   佢係由**第 0 節嗰個表自動撈出嚟**嘅，所以第 0 節嘅表一定要維持
   `| 項目 | 內容 | 來源 |` 三欄格式，否則資料格會靜靜哋變空
   （`check_site.py` 會捉，少過 6 格就當失敗）。
9. 唔用 emoji（兩隻字型都冇）。

### 四個已知字型坑（已經解決，唔好再踩）

0. **NotoSansTC 係「繁體常用字」子集，唔覆蓋 CJK 擴展區。** 說文引文入面嘅古字
   （㣇 U+38C7、𩫚 U+29ADA、𩫕 U+29AD5…）同 IPA 上古音符號（ɢ ʷ ɯ ʔ ə）佢都冇 →
   變豆腐格，而且**唔會報錯**。
   解法：HanaMinA（擴展 A）+ HanaMinB（擴展 B）做 fallback，`fontkit.py` 處理。
   **呢啲罕見字係【原文】引文嘅一部分，絕對唔可以用常見字代替** —— 咁做等於改古籍原文。
   每次加新字卡之後跑 `python3 check_glyphs.py`，佢會列出邊隻字冇字型畫得到。


1. **Chromium 出 PDF 唔食 CFF/OTF 字型** → 中文全部消失，但 PDF 照出，睇落正常。
   解法：`make_pdf_font.py` 轉做 TrueType。`check_pdf.py` 係安全網。
2. **`<code>` 預設用 monospace，monospace 冇 CJK** → `【原文】` 呢類標記變豆腐格。
   解法：CSS 明確指定 `code,pre{font-family:...}`。
3. **NotoSansTC 係純 CJK 字型，冇 `✓ ✗ ○` 呢啲符號** → 用 `*{font-family:...!important}`
   會殺埋 fallback，符號變豆腐。解法：fallback stack 留 `'DejaVu Sans'`。
   **唔好用 emoji（✅❌），兩個字型都冇。**

## 老規則（用戶定嘅）

每個回覆收尾要寫：**下一步係咩 ｜ 建議 model ｜ effort ｜ 理由**，然後**停低**等用戶轉 model
先開始執行。Plan 批准咗都要停。
